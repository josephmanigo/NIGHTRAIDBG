# Vercel web application (required for Discord sign-in and applications)
SUPABASE_URL=
SUPABASE_SECRET_KEY=
DISCORD_CLIENT_SECRET=
# Must exactly match APP_URL plus /api/auth/discord/callback and the URI
# registered in Discord Developer Portal > OAuth2 > Redirects.
DISCORD_REDIRECT_URI=https://www.nightraidbg.org/api/auth/discord/callback
# Recover the existing values when redeploying. Rotating SESSION_SECRET logs
# everyone out; rotating TOKEN_ENCRYPTION_KEY makes saved Discord grants unreadable.
SESSION_SECRET=
TOKEN_ENCRYPTION_KEY=
ADMIN_DISCORD_IDS=433544969782034442,294888876940460033
APP_URL=https://www.nightraidbg.org
APPLICATION_SIGNING_SECRET=
GEMINI_API_KEY=
# Private channel that receives application review cards and decision buttons.
DISCORD_APPLICATIONS_CHANNEL_ID=1530202289565077636

# Discord Bot Configuration
DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=1529891839484628997
DISCORD_GUILD_ID=1208444297926545489
# Required in production: stable role IDs for the two games accepted by the
# website application.
DISCORD_ROLE_BLOODSTRIKE_ID=
DISCORD_ROLE_MOBILE_LEGENDS_ID=
DISCORD_NICKNAME_CHANNEL_ID=
DISCORD_LIVE_CHANNEL_ID=
# Members earn 10 NRT once per reacted post in either of these channels.
NRT_REACTION_CHANNEL_IDS=1208607689953779712,1208609003404533780
# Private claim details are sent only to the admin channel.
DISCORD_ADMIN_CLAIM_CHANNEL_ID=1345711473476898896
# Public congratulations/status notices are sent here.
DISCORD_PUBLIC_CLAIM_CHANNEL_ID=1535215403834544158
# Calendar used by Nighty daily streaks and mission resets.
NIGHTY_TIME_ZONE=Asia/Manila
# Optional comma-separated Discord role IDs allowed to use `nighty admin ...`.
NIGHTY_ADMIN_ROLE_IDS=

# ── Webhook Server (required for Twitch EventSub & YouTube WebSub) ──
# Externally reachable HTTPS URL (e.g. https://your-bot.onrender.com)
PUBLIC_BASE_URL=
# Internal port for HTTP server (default: same as PORT or 3000)
WEBHOOK_PORT=3000

# ── Recovery / Reconciliation (NOT primary detection) ──
SOCIAL_TRACKER_RECONCILE_INTERVAL_SECONDS=300

# ── Twitch EventSub ──
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
# Random secret string for Twitch EventSub HMAC verification (you create this)
TWITCH_EVENTSUB_SECRET=

# ── YouTube Data API ──
YOUTUBE_API_KEY=

# ── TikTok (Self-Hosted Mode) ──
# Provider name: 'self-hosted' (default / free HTML parser) or custom
TIKTOK_PROVIDER=self-hosted
# Optional: Only needed if using a custom paid third-party API provider
TIKTOK_API_KEY=
TIKTOK_API_BASE_URL=
# Only set if your custom provider supports real webhook push events
TIKTOK_WEBHOOK_SECRET=
# Near-real-time polling interval (default: 10 seconds; clamped to 5-300 seconds)
TIKTOK_POLL_INTERVAL_SECONDS=10
# Coalesces command checks with nearby background polls to prevent duplicate fetches
TIKTOK_STATUS_CACHE_MS=5000
BOT_DASHBOARD_SYNC_MS=3000
# Abort an individual TikTok page request if it hangs (default: 10 seconds)
TIKTOK_REQUEST_TIMEOUT_MS=10000
# How often the upload feed is refreshed when TikTok blocks the profile page.
# Live status keeps polling at TIKTOK_POLL_INTERVAL_SECONDS; only the upload
# check falls back to this slower clock (default: 60 seconds; max 600000 ms)
TIKTOK_CONTENT_CACHE_MS=60000
